name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Google Auth
      uses: 'google-github-actions/auth@v1'
      with:
          project_id: andrewhowdencom
          workload_identity_provider: projects/422614898574/locations/global/workloadIdentityPools/github/providers/github
          service_account: github-actions-runner@andrewhowdencom.iam.gserviceaccount.com

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v1

    - name: Configure Docker
      run: gcloud auth configure-docker europe-west3-docker.pkg.dev

    - name: Build and Push
      run: |-
        docker build -t europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/ruf/ruf:latest .
        docker push europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/ruf/ruf:latest

    - name: Deploy Cloud Run Job
      run: |-
        gcloud run jobs deploy ruf-job \
          --image europe-west3-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/ruf/ruf:latest \
          --region europe-west3 \
          --service-account ruf-runner@${{ secrets.GCP_PROJECT_ID }}.iam.gserviceaccount.com

    - name: Create Cloud Scheduler Job
      run: |-
        gcloud scheduler jobs create cloud-run ruf-scheduler \
          --schedule "*/15 * * * *" \
          --job ruf-job \
          --location europe-west3 || echo "Scheduler job already exists"

    - name: Delete old Cloud Run Service
      run: gcloud run services delete ruf --region europe-west3 --quiet || echo "Service not found, skipping deletion."
